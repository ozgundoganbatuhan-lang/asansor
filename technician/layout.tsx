import { ReactNode } from "react";

export const metadata = { title: "Servisim — Teknisyen Portalı" };

export default function TechnicianLayout({ children }: { children: ReactNode }) {
  return children;
}
