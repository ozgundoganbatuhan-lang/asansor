"use client";
import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import OnboardingBeacon from "@/components/OnboardingBeacon";
import { Button, Card, EmptyState, ErrorBanner, Input, MetricCard, PageHeader, Pill, Spinner } from "@/components/ui";
type Customer = { id: string; name: string; contactName?: string | null; phone?: string | null; email?: string | null; _count: { assets: number; workOrders: number }; };
export default function CustomersPage() { const [customers, setCustomers] = useState<Customer[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [q, setQ] = useState(""); useEffect(() => { fetch("/api/customers").then((r) => r.json()).then((d) => { if (d.error) { setError(d.error); return; } setCustomers(d.items ?? []); }).catch((e) => setError(e.message)).finally(() => setLoading(false)); }, []); const filtered = useMemo(() => customers.filter((c) => !q || [c.name, c.contactName ?? "", c.phone ?? "", c.email ?? ""].join(" ").toLowerCase().includes(q.toLowerCase())), [customers, q]); const totalAssets = filtered.reduce((sum, c) => sum + c._count.assets, 0); const totalOrders = filtered.reduce((sum, c) => sum + c._count.workOrders, 0); return <div className="space-y-6"><PageHeader title="Müşteriler" subtitle="Site yönetimleri, kurumsal müşteriler ve servis ilişki geçmişi tek görünümde. Kayıt listesi kadar okunabilirlik de önemli." action={<div className="flex flex-wrap gap-2"><Link href="/app/customers/import"><Button variant="secondary">Excel yükle</Button></Link><OnboardingBeacon forStep={1} label="👆 Yeni müşteri ekleyin"><Link data-tour="new-customer-btn" href="/app/customers/new"><Button>Yeni müşteri</Button></Link></OnboardingBeacon></div>} />{error && <ErrorBanner msg={error} />}<section className="grid gap-4 lg:grid-cols-3"><MetricCard label="Portföy" value={filtered.length} note="Filtreye göre görünen müşteri sayısı" /><MetricCard label="Bağlı asansör" value={totalAssets} note="Müşteri kayıtlarına bağlı aktif varlık sayısı" /><MetricCard label="Açık iş hacmi" value={totalOrders} note="Portföy genelinde ilişkilendirilen iş emri sayısı" /></section><Card title="Müşteri listesi" subtitle="Detay, irtibat ve iş yükünü aynı tablo içinde sade biçimde sunar."><div className="mb-5 flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div className="w-full max-w-xl"><Input label="Ara" placeholder="Müşteri adı, telefon ya da ilgili kişi" value={q} onChange={(e) => setQ(e.target.value)} /></div><div className="rounded-[18px] bg-[color:var(--surface-soft-2)] px-4 py-3 text-sm text-[color:var(--muted)]">Liste sade tutuldu. İlk bakışta kimde kaç asansör ve kaç iş olduğu anlaşılsın diye.</div></div>{loading ? <Spinner /> : filtered.length === 0 ? <EmptyState icon="👥" title={q ? "Eşleşen müşteri bulunamadı" : "Henüz müşteri yok"} desc={q ? "Farklı bir arama deneyin." : "İlk müşteriyi ekleyerek portföy görünümünü oluşturun."} action={!q ? <Link href="/app/customers/new"><Button>Yeni müşteri ekle</Button></Link> : undefined} /> : <div className="table-wrap"><table className="app-table"><thead><tr><th>Müşteri</th><th>İrtibat</th><th>Telefon</th><th>Asansör</th><th>İş emri</th><th></th></tr></thead><tbody>{filtered.map((customer) => <tr key={customer.id}><td><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-[linear-gradient(135deg,#1456f0,#5ca9ff)] text-xs font-black text-white shadow-[var(--shadow-xs)]">{customer.name.split(" ").map((w) => w[0]).join("").slice(0, 2).toUpperCase()}</div><div><div className="font-bold text-[color:var(--foreground)]">{customer.name}</div><div className="mt-1 text-xs text-[color:var(--muted-2)]">{customer.email || "E-posta yok"}</div></div></div></td><td className="text-[color:var(--muted)]">{customer.contactName ?? "—"}</td><td className="text-[color:var(--muted)]">{customer.phone ?? "—"}</td><td><Pill tone="blue">{customer._count.assets} asansör</Pill></td><td>{customer._count.workOrders > 0 ? <Pill tone="amber">{customer._count.workOrders} iş emri</Pill> : <Pill tone="gray">Kayıt yok</Pill>}</td><td className="text-right"><Link href={`/app/customers/${customer.id}`} className="text-sm font-bold text-[color:var(--primary)] hover:underline">Detay</Link></td></tr>)}</tbody></table></div>}</Card></div>; }
